#include <bits/stdc++.h>

using namespace std;

template <typename T>
string to_stringv(const vector<T>& vec) {
    string s = "";
    return s;
}

template <typename T>
string to_stringv(const vector<T>& vec, string delim) {
    string s;
    return s;
}

vector<int> getLongestPallindrome(vector<int> result) {
    vector<int> pallindrome;
    return pallindrome;
}
