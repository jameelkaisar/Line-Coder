#include <bits/stdc++.h>

using namespace std;

class Unscrambler {
   private:
   public:
    Unscrambler() {
    }

    vector<int> HDB3(vector<int> bits) {
        vector<int> result;
        return result;
    }

    vector<int> B8ZS(vector<int> bits) {
        vector<int> result;
        return result;
    }
};
