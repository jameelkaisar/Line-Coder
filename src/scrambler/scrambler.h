#include <bits/stdc++.h>

using namespace std;

class Scrambler {
   private:
   public:
    Scrambler() {
    }

    vector<int> HDB3(vector<int> bits) {
        vector<int> result;
        return result;
    }

    vector<int> B8ZS(vector<int> bits) {
        vector<int> result;
        return result;
    }
};
